# `@self-healing-ci/attestation`

Supply-chain attestation-related code (SLSA / cosign-oriented; see `src/` for current scope).

```bash
pnpm --filter @self-healing-ci/attestation run build
```

See [package.json](package.json) for scripts. Root [README](../../README.md) lists workspace conventions.
