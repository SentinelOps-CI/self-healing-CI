# `@self-healing-ci/claude`

Claude API client and failure-report types used by the Temporal worker’s diagnosis activity.

## Build

```bash
pnpm --filter @self-healing-ci/claude run build
```

## Configuration

See root [.env.example](../../.env.example): `ANTHROPIC_API_KEY`, `SELF_HEALING_DRY_RUN`, and logging.
