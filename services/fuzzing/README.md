# `@self-healing-ci/fuzzing`

Differential fuzzing scaffolding (cargo fuzz, JS harnesses as implemented in `src/`).

```bash
pnpm --filter @self-healing-ci/fuzzing run build
```

See [package.json](package.json) for scripts. Root [README](../../README.md) lists workspace conventions.
