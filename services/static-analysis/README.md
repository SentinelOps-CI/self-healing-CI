# `@self-healing-ci/static-analysis`

Static analysis helpers (ESLint, Rust Clippy, Ruff, Semgrep integration as implemented in `src/`).

```bash
pnpm --filter @self-healing-ci/static-analysis run build
```

See [package.json](package.json) for scripts. Root [README](../../README.md) lists workspace conventions.
