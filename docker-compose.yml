# Local dependencies for self-healing CI (Redis). Start Temporal separately — see README.
services:
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    command: ['redis-server', '--appendonly', 'yes']
    volumes:
      - redis-data:/data

volumes:
  redis-data:
